# SilentHunter MITM Tool - Python Dependencies
# Install with: pip install -r requirements.txt

# Core networking library
scapy>=2.5.0

# System and network interface info (optional but recommended)
psutil>=5.9.0

# Network interface information (optional)
netifaces>=0.11.0

# For Windows: Npcap or WinPcap required
# Download from: https://npcap.com/
# Note: Install Npcap with "WinPcap API-compatible Mode" enabled